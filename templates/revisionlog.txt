#
# ChangeLog for $path
# 
# Generated by Trac $trac.version
# ${format_datetime}
#for item in items
  #with change = changes[item.rev]
${http_date(change.date)} $change.author [$item.rev]
    #for idx, file in enumerate(change.files)
	* $file (${dict(edit='modified', add='added', delete='deleted',
                        copy='copied', move='moved')[change.actions[idx]]})
    #end
$change.message
  #end
#end
